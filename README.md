# Assignment 1 – Stacks and Queues (Spring 2026)

This repository contains a linked-list implementation of:
- Stack (push/pop)
- Queue (enqueue/dequeue)
- Dijkstra’s two-stack algorithm to evaluate fully parenthesized expressions
- Josephus problem solver

## Build / Run
From the project root:

```bash
g++ -std=c++17 -O2 -Iinclude src/main.cpp src/ds.cpp -o app
./app
